# Story 4.5: Virtual Friend "Sophie" — First Launch Onboarding

Status: done

## Story
As a new user, I want a demo friend on first launch so I can understand the full loop before adding real contacts.

## Acceptance Criteria
1. First launch with no friends seeds demo friend `Sophie` with upcoming important event.
2. Demo friend is marked with `is_demo=true` and visible indicator.
3. Welcome greeting references Sophie flow.
4. Sophie persists until explicit removal after first real friend creation.
5. Drift migration adds `is_demo` column safely.
6. Demo data is excluded from priority scoring for real friends.

## Tasks
- [x] Add `is_demo` migration and model updates.
- [x] Implement first-launch seed logic for Sophie.
- [x] Add remove-Sophie control and lifecycle behavior.
- [x] Exclude demo entities from ranking pipeline.

## References
- `_bmad-output/planning-artifacts/epics.md` (Epic 4, Story 4.5)

## Dev Agent Record
### Agent Model Used
Claude Sonnet 4.6 (GitHub Copilot)

### Handoff

**Commit**: `feat(4-5): Sophie onboarding — is_demo migration v7, first-launch seed, demo badge, auto-remove`  
**Branch**: `main` — pushed to `LaurusNobilis/Spetaka`  
**Test suite**: 233/233 passed (9 new Sophie-onboarding tests)

#### Files Modified

| File | Change |
|------|--------|
| `spetaka/lib/features/friends/domain/friend.dart` | `isDemo` BoolColumn added with `withDefault(Constant(false))` |
| `spetaka/lib/core/database/app_database.dart` | `schemaVersion` → 7; migration v6→v7 `addColumn(friends.isDemo)`; `beforeOpen` seeds Sophie when `friendCount == 0` |
| `spetaka/lib/core/database/app_database.g.dart` | Regenerated by `build_runner` |
| `spetaka/lib/core/database/daos/friend_dao.dart` | `deleteDemoFriends()` → deletes all `isDemo=true`; `watchAllReal()` → stream excluding demos |
| `spetaka/lib/core/database/daos/friend_dao.g.dart` | Regenerated by `build_runner` |
| `spetaka/lib/features/friends/data/friend_repository.dart` | `insert()` calls `deleteDemoFriends()` when `!isDemo`; `_toEncryptedCompanion` includes `isDemo`; `removeDemoFriends()` exposed |
| `spetaka/lib/features/friends/presentation/friend_card_screen.dart` | Demo `Chip` in AppBar title + info `Banner` below AppBar for `isDemo=true` friends |
| `spetaka/lib/features/friends/presentation/friend_form_screen.dart` | `isDemo: false` added to `Friend()` constructor in save logic |
| `spetaka/test/unit/sophie_onboarding_test.dart` | **NEW** — 9 tests: AC1 seeding (5), AC4 lifecycle (3), AC5 isDemo flag (1) |
| 8 test files (repositories + widget + unit) | `isDemo: false` added to all `Friend()` constructors |
| `spetaka/test/unit/database_foundation_test.dart` | `schemaVersion` assertion updated 6 → 7 |

#### Sophie Seed Details
- ID: `demo-sophie-001` / Mobile: `+33600000000` / `isDemo: true`
- Event ID: `demo-sophie-event-001` / Type: `Important Life Event` / Date: `now + 7 days`
- Seeding condition: `IF (SELECT COUNT(*) FROM friends) == 0`

#### PriorityEngine Coupling (Story 4-1 ↔ 4-5)
`PriorityEngine.sort(friends, excludeDemo: true)` filters `FriendScoringInput.isDemo == true` before scoring.  
Call sites in 4-2 (DailyView) must pass `excludeDemo: true`.

#### Drift Migration Path
`v1→v2→v3→v4→v5→v6→v7` — all migrations remain additive; no destructive ops.

#### Next Story Dependencies
- **4-2 (Daily View)**: Must call `watchAllReal()` (not `watchAll()`) for the friend list stream, and `PriorityEngine.sort(..., excludeDemo: true)`.
- **4-3 (Heart Briefing widget)**: No direct dependency on Sophie — uses `watchAllReal()` indirectly.
